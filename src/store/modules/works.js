const works = {
    state: {
      data: []
    },
    //mutations: {
      //addWork(state, post) {
          //state.data.push(post)
      //}
    //},
    actions: {
      addNewWork({ state, rootGetters }) {
        console.log(fields)
      }
    }
  }
  
  export default works